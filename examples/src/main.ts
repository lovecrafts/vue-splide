import Vue from 'vue';
import App from './App.vue';
// import VueSplide from '@splidejs/vue-splide';

new Vue( {
  render: h => h( App ),
} ).$mount( '#app' );


// For global install
// app.use( VueSplide );

